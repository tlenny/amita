# coding=utf-8
#!/usr/bin/python3

from tqdm import tqdm
import time

pbar = tqdm(["a", "b", "c", "d",'e','f'])
for char in pbar:
    #pbar.set_description("Processing %s" % char)
    #time.sleep(1)
    pass